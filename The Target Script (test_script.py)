# test_script.py
import os
import subprocess
import socket

def normal_function():
    print("This is a legitimate application function.")
    return True

# SUSPICIOUS PATTERN 1: Hardcoded IP with socket connection (potential reverse shell)
def network_sync():
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect(("192.168.1.100", 4444)) 

# SUSPICIOUS PATTERN 2: Arbitrary code execution via eval/exec
def dynamic_execution(user_input):
    exec(user_input) 

# SUSPICIOUS PATTERN 3: Obfuscated payload execution
encoded_payload = "bndhcmU="
